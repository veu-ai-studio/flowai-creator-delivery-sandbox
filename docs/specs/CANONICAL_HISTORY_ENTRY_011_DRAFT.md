# CANONICAL_HISTORY ENTRY 011 — DRAFT

**Status:** DRAFT — pending CA-14 + CA-13 ratification per Locked Rule 13. Promotes to `docs/CANONICAL_HISTORY.md` SECTION 8 at the same commit that promotes CA-14 + CA-13.
**Author:** W3, 2026-05-18 (evening session).
**Purpose:** record the D27–D38 W5a engineering arc honestly — including the parts that ARE NOT done yet, so the canonical SSOT history does not over-claim.

This is a doc-only draft (the canonical file is not amended yet per CA-n cycle discipline). The wording below is what lands in `docs/CANONICAL_HISTORY.md` at CA-14 promotion commit, between ENTRY 010 and the trailing "END OF INVENTORY" line.

---

### ENTRY 011 — 2026-05-18

- **Session:** D27–D38 W5a engineering arc, plus parallel workstream activity (W3a Wave-1 spec v2 cohort, W5b/c orchestrator + dashboard plumbing, W2 product market definitions + migration 0015–0020 substrate).
- **Self-Renewal Phase A → architecturally complete.** The 10-module Phase A build (per ENTRY 010) is augmented in this session with five canonical safety invariants applied at the fix-generator boundary: diff-only minimal-change (commit `29ce070`); preserve rules with scoped per-finding relaxation (`41e51ed`); pre-deploy parse gate (`bbf75d9`); post-deploy regression guard (`fa9a8f0`); per-fix attribution / commit-order trace (`2f4cb97`). Build-safe fixGenerator (`481e610`) closes the Vercel-deploy root-cause. The five invariants compose into a canonical guarantee: **FlowAI never ships a fix that regresses §7.6 score or introduces new `critical`/`high` findings; it refuses the PR and exits `NO_IMPROVEMENT` with full governance-record audit trail.** Documented as CA-14-B §7 Output Contract item #6 + §10.4 Self-Governance addition (pending W6 ratification).
- **Findings-driven prioritization shipped.** The pipeline's Step 6 (govern) prioritizer was rewired (`b719c6d` D38 T1) to consume canonical §7.6 findings DIRECTLY rather than a Five-Layer-derived intermediate. The Five-Layer Intelligence Framework (§25 Locked Rule 6) remains canonical for internal classification telemetry but is NOT a remediation prioritizer. Documented as CA-14-C §6.10 + §7.6 clarification (pending W6 ratification).
- **Per-product branch-of-record + ProductSSOT seeding + atomic-audit-write substrate.** Migrations 0017–0020 (`323d5f4`, `464f65f`, `6fb0106`, `a216762`) seed FlowAI + MyPregLife product_ssot rows and add `self_renewal_branch` column to product_registry. Orchestrator + probes thread the per-product branch through (`ecf486a`). Atomic ProductSSOT write — snapshot + CAS + rollback — lands at `9b05ad7` (P0-5). Remaining 4 VEU products (SAIGE, ReachSMS, RelTwin, PressAI) pending per-product seed migrations — target: D-next dispatch. Documented as CA-14-D §7.5.1 operational invariants (pending W6 ratification).
- **§11 Step 5 four-prerequisite gate enforced.** Commit `62eacaa` W5a (P1-3) wires the ENTRY 006 §7.6 four-prerequisite gate into actual Step 5 enforcement (was canonical-doc-only prior). The four prerequisites — Report exists, score ≥band threshold, zero critical findings, Self-Renewal terminal on high — now block §11 Step 5 sign-off at runtime, not just on paper.
- **§7.6 canonical scoring becomes the loop gate.** Commit `3322a02` W5a (P0-3) makes the §7.6 canonical scorer the authoritative loop-gate score. Before this, the orchestrator's repeat-until-GTM loop used heterogeneous score sources; post-commit it uses the §7.6 canonical formula exclusively.
- **Auth-traversal wired into Aggressive Crawl Conductor.** Commit `fff3eff` W5a (P0-1) brings the AUTH_TRAVERSAL_SECURITY_SPEC v3 frozen baseline (ENTRY 007) into the live Conductor crawl path. Credentialed crawl on gated surfaces is now in production, not just spec.
- **Phase B Adversarial Surface Testing — CEO directive surfaced (NOT YET IMPLEMENTED).** CEO directive 2026-05-18 establishes Phase B (interactive adversarial verification of authenticated flows, error states, engine probes) as a HARD §11 Clearance Step 5 prerequisite. A §7.6 score WITHOUT Phase B is explicitly NOT a functional certification. The directive is captured in CA-14-A draft (pending W6 ratification). **No code yet implements Phase B**; this entry documents the canonical commitment, not a shipped capability. Implementation owner pending Panel disposition on CA-14-A-Q3.
- **Test count: 2383+ passing, zero regressions.** Across the D27–D38 arc the test suite expanded substantively (per `9cddb3b`, `f910283`, `251e140`, `a2d9df9`, `03ade29`, `2c4d3a1`, `56a46ba`, `0bf5e08`, `a0c2693`, and additional W5a/b/c test commits). No prior tests broken; every new module ships with regression coverage. **2383 is the canonical test count anchor as of D38**; subsequent dispatches will publish their delta.
- **Score-convergence status — HONESTLY: NOT YET AUTONOMOUSLY ACHIEVED.** The Phase A orchestrator can drive a §7.6 score upward through the repeat-until-GTM loop in the typical "low-hanging fruit" range (single-pass demonstration moved from 18→42 per ENTRY 010; multi-pass loops have demonstrated meaningful additional improvements). **However, the system has NOT YET autonomously achieved a sustained ≥95 §7.6 score on any VEU product end-to-end.** The current convergence ceiling is LLM-fix-quality-limited, not orchestrator-limited — the orchestrator correctly identifies findings and dispatches fixes, but the LLM's per-fix proposals stall short of the ≥95 band on findings that require multi-file coordination, semantic-aware refactoring, or domain-specific reasoning.
- **Three convergence levers documented (pending CEO decision — none implemented yet):**
  - **Lever 1 — Few-shot example diffs.** Inject curated example diffs per finding-category into the fixGenerator prompt. Hypothesis: LLM proposes higher-quality fixes when shown 2–3 canonical exemplars per category. Estimated effort: ~2 W-days to curate + wire.
  - **Lever 2 — Compound multi-stage fixes.** Decompose complex findings into multi-stage fix sequences (e.g. "first add the helper function, then update the 3 call sites, then run the test"). Hypothesis: per-stage atomic fixes pass the parse gate + regression guard more reliably than monolithic single-fix attempts. Estimated effort: ~4 W-days for orchestrator + fixGenerator extension.
  - **Lever 3 — Human-in-the-loop approval gate.** Surface high-risk fix proposals (e.g. >5-line diffs; cross-file changes) to operator for approve / modify / reject before committing to PR. Hypothesis: hybrid autonomous-with-human-gate produces production-grade quality without giving up the autonomous-velocity benefit on low-risk fixes. Estimated effort: ~1 W-day for orchestrator + UI surface; primary cost is the new Human Gate dispatch path. Aligns with §10.2 Approval Gate pattern.
  - **CEO decision pending.** Until CEO selects a lever (or some combination), the orchestrator continues producing best-effort autonomous results within the LLM-fix-quality ceiling. Documented for full disclosure; no implementation underway.
- **Parallel workstream activity:**
  - **W3a — Wave-1 spec v2 cohort.** 14/14 specs at v2 per `654291e` (Build Index updated). 9 specs revised in this session per the W3a Dispatch #2 continuation (`2fdf60d` Agent #10, `151a53b` #11, `a5bf638` #12, `d432363` #13, `2e221dd` #14, `c3090e7` #16, `2ff3d76` #17, `fa2ae4c` #18, `2ab1214` #20). All pending W6 re-ratification before Wave 1 engineering wire-in unlocks.
  - **W5b/c — Dashboard + control plane.** `5b11b0c` PAUSE/RESUME/SWITCH-MODE/STOP wiring; `71b38bd` /flowai route; `366d30b` runControlBus + /api/agent/3/control + SSE poller; `251e140` 24 control-back-channel tests; `3c5e6b4` live FlowAIDashboard; `5accddb` execute.js SSE streaming + mode/iterations/gtmTarget.
  - **W5c — Branch cleanup + structured crawl output.** `c220fda` branchCleanup automatic stale-branch reaper; `e1414d0` Vercel Cron endpoint; `b06374f` vercel.json daily cron 2am UTC; `9cddb3b` test coverage; `46bcc57` 3-consecutive-failure transient gate; `11e36e3` crawlOutputAdapter; `08403eb` +20 vendor detectors CORE/EXT split.
  - **W2 — Product market definitions canonical.** `c49f074` CANONICAL_REFERENCE §1.1 correction; `91f0c71` migration 0015 product_registry.market_definition seeded for all 5 VEU products. PERMANENT per CEO instruction 2026-05-18.
- **SSOT-vs-built drift items captured by CA-14 + CA-13 (pending W6 Panel ratification):**
  - CA-14-A — Phase B prerequisite + §7 LIMITATIONS disclosure + Locked Rule 19.
  - CA-14-B — five safety invariants + canonical guarantee + §10.4 + §12 update.
  - CA-14-C — findings-driven prioritization + §6.10 + §7.6 + Locked Rule 6 clarification.
  - CA-14-D — §7.5.1 three operational invariants (branch-of-record + seeding + atomic write).
  - CA-13-A — §7.6 GTM bar 75→95.
  - CA-13-B — §15 / §15.1 rows 21+26 CA-9-Q4 Option (a) wording reconciliation.
  - W6 Panel joint session recommended per CA-14-E.
- **Honest scope footer.** Phase A engineering substrate is solid. Phase B is committed to per CEO directive but not yet built. Autonomous self-fix to ≥95 is not yet achieved; three levers documented and awaiting CEO disposition. ENTRY 011 records the substrate + the gap honestly so SSOT history remains drift-free per Locked Rule 1.
- **Lineage:** ENTRY 010 (Phase A live build + GitHub App + Vercel preview proof) → D27–D38 W5a safety-invariant arc (`29ce070` → `b719c6d`) → migrations 0017–0020 (substrate seed) → atomic ProductSSOT write (`9b05ad7`) → P0-1/P0-3/P0-5/P1-3 enforcement (`fff3eff`, `3322a02`, `62eacaa`) → CEO Phase B directive 2026-05-18 → CA-14 draft (`fae9ff3`) + CA-13 draft (`81cf144`, prior session) → this entry.

---

*End of ENTRY 011 draft. Promotes alongside CA-14 + CA-13 promotion commits per CA-n cycle. Honest about what shipped, what's pending, and what's explicitly not yet achieved.*
