export default function CardDescriptionActionPanel() {
  return (
    <section className="space-y-2" aria-label="give the product a usable first workflow surface">
      <button className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-white hover:opacity-90">Primary actions and next steps generated from the submitted description.</button>
    </section>
  );
}
